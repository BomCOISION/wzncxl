# Python 自动化项目开发规范（精简版）

> 核心目标：高内聚、低耦合、职责单一、易维护、易扩展。

---

## 一、项目结构

```text
project/
├── main.py
├── config.py
├── steps/
├── common/
├── resources/
└── logs/
```

---

## 二、核心规则

### 1. main.py 只负责流程调度

禁止：

- 写业务逻辑
- 写点击逻辑
- 写图像识别
- 写大量 if/else

示例：

```python
def main():
    run_step_1()
    run_step_2()
```

---

### 2. 每个步骤独立文件

命名格式：

```text
step_0001_操作说明.py
```

每个步骤必须提供：

```python
def run():
    pass
```

---

### 3. 函数规范

强制要求：

- 一个函数只做一件事
- 除了main.py中的函数，其它函数不得超过 10 行
- 函数名必须使用动词开头
- 禁止重复代码
- 通用逻辑必须抽到 common/

示例：

```python
click_login_button()
wait_for_home_page()
start_game()
```

---

### 4. 配置统一管理

禁止硬编码：

- 超时时间
- 包名
- 路径
- 重试次数

统一放入：

```python
config.py
```

并添加中文注释。

---

### 5. 页面识别规范

必须使用：

```text
连续多帧截图
-> 中位值合成
-> 模板匹配
-> 连续稳定命中

脚本会先连续截图 5 帧，做中位值合成，再识别这些农场固定元素核心区域：
- 参考图只截取按钮或图标内部稳定图案
- 参考图必须完全落在目标图标或按钮的本体内部
- 如果目标图标真实大小是 32x32，参考图宽高都不得超过 32 像素
- 参考图不得包含目标图标或按钮本体外的任何像素
- 不截取透明按钮外框
- 不截取会随角色移动变化的背景区域
```

禁止：

- 单帧识别
- 匹配动态背景
- 固定 sleep 判断页面切换

---

### 6. 日志与异常

必须记录：

- 步骤开始
- 步骤结束
- 点击操作
- 错误信息

异常原则：

- common 抛异常
- steps 负责重试
- main.py 最终捕获

---

### 7. 测试规范

每次修改后只允许执行单文件或相关模块级测试：

```bash
python -m compileall -q 修改过的文件或相关目录
python 单文件测试脚本
```

禁止执行完整主流程：

```bash
python main.py
```

禁止运行会遍历账号、区服、长时间自动操作模拟器的全量流程。需要验证时，只验证本次改动涉及的单个文件、单个步骤、单个 common 模块或临时局部脚本。

---

## 三、一句话原则

> 一个文件只负责一个步骤，一个函数只负责一个动作，main.py 只负责调度。
